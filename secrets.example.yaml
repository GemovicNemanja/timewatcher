# Copy this file to secrets.yaml. The real file is gitignored.
anthropic:
  # Required for Opus reranking and catalog-description generation.
  api_key: "sk-ant-api03-replace-me"
  model: "claude-opus-4-8"

openai:
  # Required to build and query the production semantic embedding catalog.
  api_key: "sk-proj-replace-me"
  embedding_model: "text-embedding-3-small"

cloudflare:
  # Only needed by local deployment/CI; these are not Worker runtime secrets.
  account_id: "replace-me"
  api_token: "replace-me"

search:
  cache_ttl_seconds: 3600
